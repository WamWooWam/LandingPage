import { Attributes, Component, ComponentChild, ComponentChildren, Ref } from "preact";

export default class LockScreenPage extends Component {
    render() {
        return (
            <div class="main-view">
                <h2 class="section-header">Lock Screen</h2>
            </div>
        );
    }
}